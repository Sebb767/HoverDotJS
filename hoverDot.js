/**
 * Created by sebastian.kaim on 26.04.2016.
 */

(function ($) {

    // register the plugin
    $.fn.dotHover = function (options) {

        var settings = $.extend({
            // defaults
            

        }, options );

        return this.each(function() {
            // init each element
        });
    };

})(jQuery);
